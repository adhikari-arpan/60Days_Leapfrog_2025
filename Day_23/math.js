function add(a,b) {
    return a + b;
}

function sub(a,b) {
    return a - b;
}

module.exports = {
    add, 
    sub
}; // Exporting functions named add and sub

// exports.add = (a, b) => a + b;
// exports.subtract = (a, b) => a - b;
